import React from "react";
// import "./loading.scss";

function Loading() {
  return (
    <div
      style={{
        width: "100%",
        paddingBlock: "15px",
        paddingBottom: "100px",
        display: "flex",
        justifyContent: "center",
      }}
    >
      <div>Loading...</div>
    </div>
  );
}

export default Loading;
